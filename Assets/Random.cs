using UnityEngine;

// Renamed from 'Random' — the name 'Random' shadows UnityEngine.Random and breaks all
// scripts that call Random.Range without a full namespace qualifier.
public class RandomHelper : MonoBehaviour
{
    void Start() { }
    void Update() { }
}
